<%= form_with(model: job_offer) do |form| %>
  <% if job_offer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_offer.errors.count, "error") %> prohibited this job_offer from being saved:</h2>

      <ul>
        <% job_offer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :puesto, style: "display: block", class:"label" %>
    <%= form.text_field :title , class:"contenido" %>
  </div>

  <div>
    <%= form.label :descripción, style: "display: block" , class:"label" %>
    <%= form.text_area :description, class:"contenido" %>
  </div>

  <div>
    <%= form.submit  "ENVIAR", class:"boton-enviar"%>
  </div>
<% end %>

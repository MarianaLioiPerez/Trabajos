<%= form_with(model: @aplication) do |form| %>
  <% if @aplication&.errors&.any? %>
    <div style="color: red">
      <h2><%= pluralize(@aplication.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
        <% @aplication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.label :job_offer_id, style: "display: block" %>
    <%= form.text_field :job_offer_id %>
  </div>

  <%= form.hidden_field :tuser_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>